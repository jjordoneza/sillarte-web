---
// Componente de imagen con marcador de posición.
// Mientras las fotos reales no existan en /public/products o /public/scenes,
// muestra una etiqueta descriptiva en lugar del ícono de imagen rota.
interface Props {
  src: string;
  alt: string;
  marcador: string;
  class?: string;
  loading?: "lazy" | "eager";
  fetchpriority?: "high" | "low" | "auto";
  // Permite atributos data-* arbitrarios (p. ej. data-galeria-principal
  // para engancharle un script desde la página que use este componente).
  [key: `data-${string}`]: string | boolean | undefined;
}

const { src, alt, marcador, class: clase = "", loading = "lazy", fetchpriority = "auto", ...resto } = Astro.props;
---

<div class:list={["medio", clase]} {...resto}>
  <img
    src={src}
    alt={alt}
    loading={loading}
    fetchpriority={fetchpriority}
    decoding="async"
    onerror="this.classList.add('medio__img--error')"
  />
  <div class="medio__marcador" aria-hidden="true">
    <span>Foto pendiente</span>
    <span>{marcador}</span>
  </div>
</div>
